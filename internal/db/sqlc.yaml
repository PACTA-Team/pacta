version: "2"
sql:
  - schema: "internal/db/migrations/*.sql"
    queries: "internal/db/queries/*.sql"
    engine: "sqlite"
    gen:
      go:
        package: "db"
        out: "."
        emit_interface: true  # Genera interfaz Queries para poder mockear en tests y usar WithTx
