version: "2"
sql:
  - schema: "internal/db/migrations/*.sql"
    queries: "internal/db/queries/*.sql"
    engine: "sqlite"
    gen:
      go_package:
        mode: "query"
        name: "db"
      emit:
        interface: true
        # Genera interfaz Queries para poder mockear en tests y usar WithTx
